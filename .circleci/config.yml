version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run: npm run test
      - run:
          name: exit with 1
          command: |
            sh ./simple_script.sh
